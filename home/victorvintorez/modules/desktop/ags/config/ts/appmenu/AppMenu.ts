import { Widget } from "../../js/imports";


const AppMenu = Widget.Box({
    name: "AppMenu",
    children: [
        Widget.Button({
            name: "Button1",
            text: "Button1",
        }),
    ],
})

export default AppMenu;